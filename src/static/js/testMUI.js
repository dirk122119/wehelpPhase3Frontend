import React from 'react';
import {Button} from '@mui/material';

function TestMuiButton() {
    return (
        <div>
            <div>HI</div>
            <Button variant="contained">測試ＭＵＩ</Button>
        </div>
    );
  }

export {TestMuiButton}